{
  "rules": {
    "locations": {
      "$agencyId": {
        
        ".read": "auth != null",
        "$agentId": {
          
          ".write": "auth != null && auth.uid === $agentId"
        }
      }
    }
  }
}